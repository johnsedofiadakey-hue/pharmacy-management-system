# Dev Brain shared project memory

Activation: Always On

This workspace is coordinated by Dev Brain for **pharmacy management system**.
Read `@../../.devbrain/context.md` before planning or changing code.
Use it as the shared project memory across Antigravity, Codex, and Claude.
Read `@../../.devbrain/handoffs.md` for cross-agent results recorded outside this workspace.
Keep work scoped to the current outcome and verify relevant tests before reporting completion.
Do not commit, push, deploy, or modify credentials unless the user explicitly requests it.
If repository state conflicts with shared memory, trust the repository and report the mismatch.
